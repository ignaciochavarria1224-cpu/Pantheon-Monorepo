---
id: 303
date: 2026-03-23
tag: ChatGPT
---

[Folder and File Setup]

The main issue is not prompt creativity anymore.

The main issue is that the LLM generator is not truly learning from previous mistakes and taking them into account strongly enough.

Right now it reads history, but it is still behaving more like context-aware generation than mistake-aware generation.

What I need now:
turn `apex_adapt/research/llm_generator.py` into a failure-aware generator.

Specifically, I want the generator to use past failures as strong negative guidance.

Current problem:
- it keeps generating ideas that sound plausible
- but many die in pre-screen
- and it is not aggressively avoiding shapes that already failed recently

I want 4 upgrades:

1. Recent failure memory
   - Read recent LLM-generated strategies from research history
   - Focus especially on the last 10–20 LLM strategies
   - Extract:
     - strategy name
     - archetype
     - entry rules
     - actual trade count
     - classification
     - failure reasons
   - Summarize what just failed recently

2. Failure pattern summarization
   - Build interpreted summaries like:
     - “breakout_flag + rvol + score_normalized repeatedly fails pre-screen”
     - “VWAP corridor + volume + quality is usually too restrictive”
     - “simple roc_10 + vwap ideas survive better than overloaded 4-rule conjunctions”
   - Use these as explicit negative constraints in the prompt

3. Strong negative prompting
   - The prompt should explicitly tell Claude:
     - avoid near-duplicates of recent LLM failures
     - avoid combinations with repeated low conversion to quarantine
     - avoid shapes that repeatedly lead to 0–5 trades
   - This should be much stronger than the current novelty language

4. Outcome feedback structure
   - Add a helper that converts prior research reports into generator feedback:
     - recent_llm_failures
     - recent_llm_survivors
     - repeated_failure_shapes
     - repeated_success_shapes
   - Use that structured feedback in prompt construction

Important constraints:
- do not redesign the whole system
- do not touch unrelated files
- keep the same public interface:
  - `llm_generate_batch(...)`
  - `llm_mixed_batch(...)`
- preserve safe fallback behavior
- return production-ready code

What I want from you:
1. First explain why the current generator is not learning strongly enough from mistakes.
2. Then explain the exact design upgrade.
3. Then return the full updated `apex_adapt/research/llm_generator.py`.
4. Then explain what changed and why it should improve learning from failures.
5. Do not give vague ideas only — give the actual full file.
