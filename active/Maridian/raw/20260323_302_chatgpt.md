---
id: 302
date: 2026-03-23
tag: ChatGPT
---

[Folder and File Setup]

Now I have everything. I'm building a Claude-powered strategy generator that actually reasons about markets instead of picking from a list.Now wire it into `run_autonomous_research.py` as the primary generator:Here's exactly what this builds and why it matters.

---

**What `llm_generator.py` does differently:**

Every time Olympus runs a research cycle, instead of picking `roc_5 + rvol` from a fixed list for the 700th time, Claude receives:

- The full glossary of what each feature actually measures in SPY 5-min bars
- A summary of every promoted strategy (with rules and why they passed)
- The best quarantine strategies and what they have in common
- The most common failure reasons across 700+ tests
- Feature frequency by classification (which features appear in winners vs losers)
- Specific knowledge about SPY intraday behavior — momentum windows, VWAP dynamics, lunch hour patterns

Claude then reasons about microstructure and proposes hypotheses it believes have theoretical edge, not combinations it randomly sampled.

---

**Install it:**

```powershell
copy "%USERPROFILE%\Downloads\llm_generator.py" apex_adapt\research\llm_generator.py
```

**Apply the patch to `run_autonomous_research.py`:**

Open it in VS Code, add the import line after the existing two generator imports, then replace the `hypotheses = optuna_generate_batch(...)` call with the branching logic in the patch file.

**Verify:**
```powershell
python -c "import py_compile; py_compile.compile('apex_adapt/research/llm_generator.py'); print('OK')"
python -c "import py_compile; py_compile.compile('run_autonomous_research.py'); print('OK')"
```

**Test one cycle:**
```powershell
python olympus.py --count 5
```

You'll see in the Stage 1 output lines like `LLM GENERATOR | Requesting 3 strategies from Claude` followed by the actual strategy names Claude invented. Each one will have a real `thesis` and `market_logic` that explains the reasoning, not a template string.

The cost is roughly $0.03-0.05 per cycle of 5-10 strategies. After tomorrow's market session, run a cycle and paste the output — I want to see what Claude proposes when it reads 700 experiments of context.
